<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <RootNamespace>fajlbololvasas_teszt</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Add this ItemGroup to copy narancs.txt to the output folder -->
  <ItemGroup>
    <None Include="narancs.txt" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
